# Copy this to `.env.production` on the server and fill in real values.
# (Do NOT commit the filled version — it contains secrets.)

# Your MongoDB Atlas connection string
MONGODB_URI=

# Admin login for the dashboard
ADMIN_EMAIL=ziyarmajlis@gmail.com
ADMIN_PASSWORD=

# Session signing secret — generate with:  openssl rand -hex 32
JWT_SECRET=

NODE_ENV=production
